diff --git a/app/modules/proxy/api.py b/app/modules/proxy/api.py
index 34b018d..0f0d737 100644
--- a/app/modules/proxy/api.py
+++ b/app/modules/proxy/api.py
@@ -193,6 +193,7 @@ async def v1_responses(
             codex_session_affinity=False,
             openai_cache_affinity=True,
             prefer_http_bridge=True,
+            public_openai_sse_only=True,
         )
     return await _collect_responses(
         request,
@@ -202,6 +203,7 @@ async def v1_responses(
         codex_session_affinity=False,
         openai_cache_affinity=True,
         prefer_http_bridge=True,
+        public_openai_sse_only=True,
     )
 
 
@@ -433,6 +435,7 @@ async def _stream_responses(
     openai_cache_affinity: bool = False,
     suppress_text_done_events: bool = False,
     prefer_http_bridge: bool = False,
+    public_openai_sse_only: bool = False,
 ) -> Response:
     apply_api_key_enforcement(payload, api_key)
     validate_model_access(api_key, payload.model)
@@ -476,6 +479,8 @@ async def _stream_responses(
             api_key_reservation=reservation,
             suppress_text_done_events=suppress_text_done_events,
         )
+    if public_openai_sse_only:
+        stream = _filter_public_openai_responses_events(stream)
     try:
         first = await stream.__anext__()
     except StopAsyncIteration:
@@ -509,6 +514,7 @@ async def _collect_responses(
     openai_cache_affinity: bool = False,
     suppress_text_done_events: bool = False,
     prefer_http_bridge: bool = False,
+    public_openai_sse_only: bool = False,
 ) -> Response:
     apply_api_key_enforcement(payload, api_key)
     validate_model_access(api_key, payload.model)
@@ -552,6 +558,8 @@ async def _collect_responses(
             api_key_reservation=reservation,
             suppress_text_done_events=suppress_text_done_events,
         )
+    if public_openai_sse_only:
+        stream = _filter_public_openai_responses_events(stream)
     try:
         response_payload = await _collect_responses_payload(stream)
     except ProxyResponseError as exc:
@@ -737,6 +745,17 @@ def _parse_sse_payload(line: str) -> dict[str, JsonValue] | None:
     return parse_sse_data_json(line)
 
 
+def _is_public_openai_responses_event_type(event_type: JsonValue) -> bool:
+    return event_type == "error" or (isinstance(event_type, str) and event_type.startswith("response."))
+
+
+async def _filter_public_openai_responses_events(stream: AsyncIterator[str]) -> AsyncIterator[str]:
+    async for line in stream:
+        payload = _parse_sse_payload(line)
+        if payload is None or _is_public_openai_responses_event_type(payload.get("type")):
+            yield line
+
+
 def _logged_error_json_response(
     request: Request,
     status_code: int,
diff --git a/tests/integration/test_proxy_responses.py b/tests/integration/test_proxy_responses.py
index 92281be..d9d0d1a 100644
--- a/tests/integration/test_proxy_responses.py
+++ b/tests/integration/test_proxy_responses.py
@@ -685,6 +685,91 @@ async def test_v1_responses_non_streaming_reconstructs_reasoning_output(async_cl
     ]
 
 
+@pytest.mark.asyncio
+async def test_v1_responses_streaming_filters_private_sideband_events(async_client, monkeypatch):
+    email = "responses-stream-filter@example.com"
+    raw_account_id = "acc_responses_stream_filter"
+    auth_json = _make_auth_json(raw_account_id, email)
+    files = {"auth_json": ("auth.json", json.dumps(auth_json), "application/json")}
+    response = await async_client.post("/api/accounts/import", files=files)
+    assert response.status_code == 200
+
+    async def fake_stream(payload, headers, access_token, account_id, base_url=None, raise_for_status=False, **_kw):
+        yield 'data: {"type":"codex.rate_limits","rate_limits":{"allowed":true},"sequence_number":0}\n\n'
+        yield (
+            'data: {"type":"response.created","response":{"id":"resp_stream_filter_1","object":"response",'
+            '"status":"in_progress","output":[]}}\n\n'
+        )
+        yield (
+            'data: {"type":"response.completed","response":{"id":"resp_stream_filter_1","object":"response",'
+            '"status":"completed","output":[],"usage":{"input_tokens":1,"output_tokens":1,"total_tokens":2}}}\n\n'
+        )
+
+    monkeypatch.setattr(proxy_module, "core_stream_responses", fake_stream)
+
+    payload = {"model": "gpt-5.1", "input": "hi", "stream": True}
+    async with async_client.stream("POST", "/v1/responses", json=payload) as resp:
+        assert resp.status_code == 200
+        lines = [line async for line in resp.aiter_lines() if line]
+
+    events = [json.loads(line[6:]) for line in lines if line.startswith("data: ")]
+    assert [event["type"] for event in events] == ["response.created", "response.completed"]
+
+
+@pytest.mark.asyncio
+async def test_v1_responses_non_streaming_ignores_private_sideband_events(async_client, monkeypatch):
+    email = "responses-nonstream-filter@example.com"
+    raw_account_id = "acc_responses_nonstream_filter"
+    auth_json = _make_auth_json(raw_account_id, email)
+    files = {"auth_json": ("auth.json", json.dumps(auth_json), "application/json")}
+    response = await async_client.post("/api/accounts/import", files=files)
+    assert response.status_code == 200
+
+    async def fake_stream(payload, headers, access_token, account_id, base_url=None, raise_for_status=False, **_kw):
+        yield 'data: {"type":"codex.rate_limits","rate_limits":{"allowed":true},"sequence_number":0}\n\n'
+        yield (
+            'data: {"type":"response.completed","response":{"id":"resp_nonstream_filter_1","object":"response",'
+            '"status":"completed","output":[],"usage":{"input_tokens":1,"output_tokens":1,"total_tokens":2}}}\n\n'
+        )
+
+    monkeypatch.setattr(proxy_module, "core_stream_responses", fake_stream)
+
+    payload = {"model": "gpt-5.1", "input": "hi", "stream": False}
+    resp = await async_client.post("/v1/responses", json=payload)
+
+    assert resp.status_code == 200
+    body = resp.json()
+    assert body["id"] == "resp_nonstream_filter_1"
+    assert body["status"] == "completed"
+
+
+@pytest.mark.asyncio
+async def test_backend_responses_streaming_preserves_private_sideband_events(async_client, monkeypatch):
+    email = "responses-backend-sideband@example.com"
+    raw_account_id = "acc_responses_backend_sideband"
+    auth_json = _make_auth_json(raw_account_id, email)
+    files = {"auth_json": ("auth.json", json.dumps(auth_json), "application/json")}
+    response = await async_client.post("/api/accounts/import", files=files)
+    assert response.status_code == 200
+
+    async def fake_stream(payload, headers, access_token, account_id, base_url=None, raise_for_status=False, **_kw):
+        yield 'data: {"type":"codex.rate_limits","rate_limits":{"allowed":true},"sequence_number":0}\n\n'
+        yield (
+            'data: {"type":"response.completed","response":{"id":"resp_backend_sideband_1","object":"response",'
+            '"status":"completed","output":[],"usage":{"input_tokens":1,"output_tokens":1,"total_tokens":2}}}\n\n'
+        )
+
+    monkeypatch.setattr(proxy_module, "core_stream_responses", fake_stream)
+
+    payload = {"model": "gpt-5.1", "instructions": "hi", "input": [], "stream": True}
+    async with async_client.stream("POST", "/backend-api/codex/responses", json=payload) as resp:
+        assert resp.status_code == 200
+        lines = [line async for line in resp.aiter_lines() if line]
+
+    events = [json.loads(line[6:]) for line in lines if line.startswith("data: ")]
+    assert [event["type"] for event in events] == ["codex.rate_limits", "response.completed"]
+
+
 @pytest.mark.asyncio
 async def test_v1_responses_non_streaming_preserves_sse_error_payload(async_client, monkeypatch):
     email = "responses-error-event@example.com"
